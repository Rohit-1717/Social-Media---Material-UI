import { createTheme } from "@mui/material/styles";

 const theme = createTheme({
  palette: {
    primary: {
      main: "#557C56",
      light: "skyblue",
    },
    secondary: {
      main: "#000",
    },
    otherColor: {
      main: "#CD5C08"
    },
  },
});

export default theme;